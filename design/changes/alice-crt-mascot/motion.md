# Motion Specialization

Uses the project `transform.orbit` primitive as a 3-second, 12-frame raster micro-orbit. The full artwork moves no more than 2 px from its resting crop so the CRT composition remains readable. It communicates a live-but-stable terminal signal, not task progress.

The static PNG is the explicit reduced-motion substitute.
